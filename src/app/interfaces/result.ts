export interface result {
    playerId: number;
    totalScore: number;
    gamesPlayed: number;
  }
  
 export interface mappedData {
    name: string;
    totalScore: number;
    gamesPlayed: number;
  }